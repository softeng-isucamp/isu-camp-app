# Direct dependencies only; pip resolves the rest.
#
# The previous file also pinned matplotlib, pandas, scikit-learn, scipy, numpy,
# pillow, joblib and requests. Nothing under app/ or tests/ imports any of them,
# so they never cost run time - but a fresh `pip install -r` pulled the whole
# scientific stack, which is minutes of build time and hundreds of MB of image.

fastapi==0.141.1
uvicorn==0.52.4
supabase==2.31.0
pydantic==2.13.4
pwdlib[argon2]==0.3.1
PyJWT==2.13.0
python-dotenv==1.2.3
email-validator==2.3.0

# fastapi.testclient (tests only) needs httpx, which supabase already requires.
httpx==0.28.1
