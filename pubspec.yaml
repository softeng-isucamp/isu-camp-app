name: isu_camp_app
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  device_preview: ^1.3.1
  google_fonts: ^8.2.1
  geolocator: ^13.0.1
  flutter_compass: ^0.8.1
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  http: ^1.6.0
  shared_preferences: ^2.5.3
  crypto: ^3.0.6
  flutter_secure_storage: ^11.2.0
  connectivity_plus: ^6.1.5
  path_provider: ^2.1.6
  archive: ^4.0.7
  flutter_onnxruntime: ^1.8.5
  dart_sentencepiece_tokenizer: ^1.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/offline_tiles/campus_tiles.zip
